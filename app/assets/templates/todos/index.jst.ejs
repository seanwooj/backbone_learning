<h3>Index!</h3>

<ul>
  <% todos.each(function(todo){ %>
    <!-- still using get! -->
    <li>
      <a href="#/todos/<%= todo.id %>">
        <%= todo.escape("title") %>
      </a>
    </li>
  <% }); %>
</ul>

<a href="#/todos/new">New Todo</a>