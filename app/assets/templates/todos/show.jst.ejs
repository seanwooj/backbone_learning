<h3><%= todo.escape("title") %></h3>

<ul>
  <% todo.comments().each(function (comment) { %>
    <li><%= comment.escape("content") %></li>
  <% }); %>
</ul>

<div class="comment-new"></div>

<a href="#/">Back to Index</a>